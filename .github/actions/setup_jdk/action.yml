name: Setup Java SDK
description: setup shared JDK environment for build use.

runs:
  using: "composite"
  steps:
    - uses: actions/setup-java@v4
      with:
        distribution: temurin
        java-version: 17
