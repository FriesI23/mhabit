name: mhabit_adaptive_ui
description: Adaptive UI components and layout infrastructure for mhabit.
publish_to: none

resolution: workspace

environment:
  sdk: ">=3.12.0 <4.0.0"
  flutter: ">=3.44.0"

dependencies:
  adaptive_actions: ^0.3.0
  flutter:
    sdk: flutter
  ios_window_control_layout: ^0.3.0

dev_dependencies:
  flutter_test:
    sdk: flutter
